# Copy to .env. Only the Entrez pair is required, and only for the GEO sources.
ENTREZ_EMAIL=you@example.com     # NCBI asks every caller to identify itself
NCBI_API_KEY=                    # optional, raises the Entrez rate limit
