numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
openml>=0.14
GEOparse>=2.0
biopython>=1.81
requests>=2.31
kaggle>=1.6
ucimlrepo>=0.0.7
tabpfn >=2.0,<3  # TabPFN v2 (2.x); 6.x+ packages pull the v3 model
plotly>=5.0
rdkit

# NOTE: microbiome datasets come from curatedMetagenomicData, an R/Bioconductor
# package — NOT pip-installable. It lives in environment.yml (conda, bioconda channel)
# and must be installed on the cluster. Stage its data with scripts/export_cmd.R
# (R) then scripts/cmd_prep.py.
